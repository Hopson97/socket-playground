name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Go to UDP Rect Keys
      run: cd udp-rect-keys
    - name: build
      run: sh scripts/build.sh

